apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: haproxy-server-ingress
  namespace: haproxy-demo
spec:
  podSelector:
    matchLabels:
      app: haproxy-server
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - protocol: TCP
          port: 8443
